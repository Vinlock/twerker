/**
 * Twerker - TypeScript Worker Threads Made Simple
 * Main package entry point
 */

import run, { Worker, WorkerPool, PoolOptions, TwerkerOptions } from './twerker';

export default run;
export { Worker, WorkerPool, PoolOptions, TwerkerOptions }; 